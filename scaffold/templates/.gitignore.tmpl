# Dependencies
node_modules/

# Patches (exported from worktrees, applied to external repos)
patches/

# OS
.DS_Store

# Repos (clean clones — large, not tracked)
repos/

# Worktrees (git worktrees — managed by git, not tracked here)
workspaces/
